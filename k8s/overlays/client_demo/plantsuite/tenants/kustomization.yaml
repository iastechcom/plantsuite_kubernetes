namespace: plantsuite

resources:
  - ../../../../base/plantsuite/tenants

patches:
  - target:
      kind: Deployment
      name: tenants
    path: patches/deployment-ops.yaml
  - path: patches/virtualservice-tenants.yaml
    target:
      kind: VirtualService
      name: tenants
